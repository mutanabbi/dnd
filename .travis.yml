language: cpp

dist:
  - bionic

compiler:
  - clang
  - gcc

before_install:
  - sudo apt-get -y install apt-transport-https ca-certificates gnupg software-properties-common wget
  - wget -O - https://apt.kitware.com/keys/kitware-archive-latest.asc 2>/dev/null | sudo apt-key add
  - sudo apt-add-repository 'deb https://apt.kitware.com/ubuntu/ bionic main'
  - sudo apt-get update
  - sudo apt-get -y install cmake cmake-data libreadline-dev libboost-dev libgtest-dev
  - PATH=`sed 's,/usr/local/cmake-3\.12\.4/bin/:,,' <<< "${PATH}"`
  - echo ${PATH}
  - which cmake
  - (cd /usr/src/gtest && sudo sh -c "cmake . && make -j && make install")

script: echo $PATH && mkdir build && (echo $PATH && cd build && cmake ../ && make -j && make test)

